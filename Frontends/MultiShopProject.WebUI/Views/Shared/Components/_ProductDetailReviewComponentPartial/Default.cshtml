@model List<ResultCommentDto>

<div class="tab-pane fade" id="tab-pane-3">
    <div class="row">
        <div class="col-md-6">
            <h4 class="mb-4">@Model.Count yorum bulunmaktadır</h4>
            @foreach (var item in Model)
            {
                <div class="media mb-4">
                    <img src="@item.ImageUrl" alt="Image" class="img-fluid mr-3 mt-1" style="width: 45px;">
                    <div class="media-body">
                        <h6>@item.Fullname<small> - <i>@item.CreatedDate.ToShortDateString()</i></small></h6>
                        <div class="text-primary mb-2">
                            @for(int i = 0; i < 5; i++)
                            {
                                if (i < item.Rating)
                                {
                                    <i class="fas fa-star"></i>
                                }
                                else
                                {
                                    <i class="far fa-star"></i>
                                }
                            }
                        </div>
                        <p>@item.CommentDetail</p>
                    </div>
                </div>
            }
        </div>
        @await Html.PartialAsync("~/Views/ProductList/AddComment.cshtml", new { id = ViewBag.x })
        @* <div class="col-md-6">
            <h4 class="mb-4">Bir Yorum Bırakın</h4>
            <small>Yorumunuzun alınması için geçerli bir e-mail adresi giriniz. Zorunlu alanlar * ile belirtilmiştir.</small>
            <div class="d-flex my-3">
                <p class="mb-0 mr-2">Ürün Puanınız * :</p>
                <div class="text-primary">
                    <i class="far fa-star"></i>
                    <i class="far fa-star"></i>
                    <i class="far fa-star"></i>
                    <i class="far fa-star"></i>
                    <i class="far fa-star"></i>
                </div>
            </div>
            <form>
                <div class="form-group">
                    <label for="message">Yorumunuz *</label>
                    <textarea id="message" cols="30" rows="5" class="form-control"></textarea>
                </div>
                <div class="form-group">
                    <label for="name">Adınız ve soyadınız *</label>
                    <input type="text" class="form-control" id="name">
                </div>
                <div class="form-group">
                    <label for="email">E-mail adresiniz *</label>
                    <input type="email" class="form-control" id="email">
                </div>
                <div class="form-group mb-0">
                    <input type="submit" value="Yorum Yap" class="btn btn-primary px-3">
                </div>
            </form>
        </div> *@
    </div>
</div>